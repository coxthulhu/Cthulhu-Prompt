/** Preferred viewport-top offset for prompt and template navigation. */
export const PROMPT_FOLDER_VERTICAL_BIAS_PX = 300

/** Viewport-top offset retained for category navigation. */
export const PROMPT_FOLDER_CATEGORY_TOP_OFFSET_PX = 80
